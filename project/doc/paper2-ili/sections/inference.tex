\section{Inference Framework}
\label{sec:inference}

\subsection{Inference Targets}

\textbf{[PLACEHOLDER]} The inference problem is: given observed \PHANGS\ summary
statistics $\mathbf{x}_{\rm obs}$ at an aperture, infer the posterior distribution
over the environmental and physical parameters $\thetaenv$ (and eventually $\thetaphys$).

\subsection{Neural Posterior Estimation}

\textbf{[PLACEHOLDER]} We use a neural posterior estimator (NPE) trained on
simulation--observation pairs $(\thetaenv^{(k)}, \mathbf{x}^{(k)})$ from the
\TIGRESS-NCR\ suite.

[Architecture, training procedure, and hyperparameters TBD]

\subsection{Training and Validation}

\textbf{[PLACEHOLDER]}

\subsection{Posterior Diagnostics}

\textbf{[PLACEHOLDER]} Simulation-based calibration (SBC), coverage tests,
expected coverage probability (ECP) checks.
